name: Build And Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.goos }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-latest
            goos: windows
            goarch: amd64
            output: awan-windows-amd64.exe
          - runner: ubuntu-latest
            goos: linux
            goarch: amd64
            output: awan-linux-amd64

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install dependencies
        run: go mod tidy

      - name: Create dist directory
        run: mkdir dist

      # Build the single AWaN runtime binary for the target OS/arch.
      - name: Build application
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: go build -trimpath -ldflags "-s -w" -o dist/${{ matrix.output }} .

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.goos }}
          path: dist/*
          if-no-files-found: error

  release:
    name: Create GitHub release
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: dist-windows
          path: dist

      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: dist-linux
          path: dist

      - name: Create or update release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: auto-${{ github.run_number }}
          RELEASE_NAME: Build ${{ github.run_number }}
        run: |
          set -euo pipefail

          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            gh release upload "$TAG_NAME" dist/*.exe dist/*linux* --clobber
          else
            gh release create "$TAG_NAME" \
              dist/*.exe \
              dist/*linux* \
              --title "$RELEASE_NAME" \
              --notes "Automated build for run ${{ github.run_number }}" \
              --target "${{ github.sha }}"
          fi
